{% extends "template.html" %}

{% block head %}
	<title>Dashboard - Poolcar</title>
	<style>
		.icon-box {
			width: 60px;
			height: 60px;
			border-radius: 12px;
			display: flex;
			align-items: center;
			justify-content: center;
		}
	</style>
{% endblock %}

{% block body %}
<div class="d-flex flex-column gap-4" style="height: calc(100vh - 120px);">
	<!-- Page Header -->
	<div class="flex-shrink-0">
		<h3 class="fw-bold text-dark">Dashboard</h3>
		<p class="text-dark-emphasis">Welcome back, {{ username }}</p>
	</div>

	<!-- Information Cards -->
	<div class="row g-4 flex-shrink-0">
		<!-- Card 1: Active Activities -->
		<div class="col-3">
			<div class="card border-0 shadow-sm h-100">
				<div class="card-body p-4 d-flex align-items-center gap-3">
					<div class="icon-box text-bg-primary">
						<img src="/assets/trip_bold.svg" alt="Active Activities" width="40" height="40">
					</div>
					<div>
						<div class="text-dark-emphasis fw-semibold">Active Activities Today</div>
						<div class="fs-4 text-dark fw-bold">3 <span class="fs-6 text-dark-emphasis fw-normal">/ 12 total</span></div>
					</div>
				</div>
			</div>
		</div>

		<!-- Card 2: Cars with Assigned Trackers -->
		<div class="col-3">
			<div class="card border-0 shadow-sm h-100">
				<div class="card-body p-4 d-flex align-items-center gap-3">
					<div class="icon-box text-bg-success">
						<img src="/assets/cars_bold.svg" alt="Cars" width="40" height="40">
					</div>
					<div>
						<div class="text-dark-emphasis fw-semibold">Cars with Assigned Trackers</div>
						<div class="fs-4 text-dark fw-bold">8 <span class="fs-6 text-dark-emphasis fw-normal">/ 10 cars</span></div>
					</div>
				</div>
			</div>
		</div>

		<!-- Card 3: Total Trackers -->
		<div class="col-3">
			<div class="card border-0 shadow-sm h-100">
				<div class="card-body p-4 d-flex align-items-center gap-3">
					<div class="icon-box text-bg-warning">
						<img src="/assets/trackers_bold.svg" alt="Total Trackers" width="40" height="40">
					</div>
					<div>Main Row
						<div class="text-dark-emphasis fw-semibold">Total Trackers</div>
						<div class="fs-4 text-dark fw-bold">15</div>
					</div>
				</div>
			</div>
		</div>

		<!-- Card 4: Car History -->
		<div class="col-3">
			<div class="card border-0 shadow-sm h-100">
				<div class="card-body p-4 d-flex align-items-center gap-3">
					<div class="icon-box text-bg-info">
						<img src="/assets/car_history_bold.svg" alt="Car Checks Today" width="24" height="24">
					</div>
					<div>
						<div class="text-dark-emphasis fw-semibold">Car History</div>
						<div class="fs-4 text-dark fw-bold">45</div>
					</div>
				</div>
			</div>
		</div>
	</div>

	<!-- Main Row -->
	<div class="row g-4 flex-grow-1" style="min-height: 0;">
		<!-- Left Side: Recent Activities -->
		<div class="col-4 d-flex flex-column">
			<div class="card border-0 shadow-sm flex-grow-1 d-flex flex-column overflow-hidden p-4">
				<div class="card-header border-0 bg-transparent p-0 mb-2 flex-shrink-0">
					<h5 class="fw-bold text-dark mb-0">Recent Activities</h5>
				</div>
				<div class="card-body flex-grow-1 p-0">
					<div class="list-group list-group-flush">
						<!-- Item 1 -->
						<div class="list-group-item py-3">
							<div class="d-flex align-items-center justify-content-between">
								<span class="badge bg-success-subtle text-success rounded-pill">Active</span>
								<small class="text-dark-emphasis">10 mins ago</small>
							</div>
							<h6 class="fw-semibold text-dark mt-2 mb-1">Toyota Avanza (B 1234 CD)</h6>
							<p class="text-dark-emphasis m-0">Heading to: Kantor Pusat Jakarta</p>
						</div>
						<!-- Item 2 -->
						<div class="list-group-item py-3">
							<div class="d-flex align-items-center justify-content-between">
								<span class="badge bg-success-subtle text-success rounded-pill">Active</span>
								<small class="text-dark-emphasis">35 mins ago</small>
							</div>
							<h6 class="fw-semibold text-dark mt-2 mb-1">Honda CR-V (B 5678 EF)</h6>
							<p class="text-dark-emphasis m-0">Heading to: Bandara Soekarno-Hatta</p>
						</div>
						<!-- Item 3 -->
						<div class="list-group-item py-3">
							<div class="d-flex align-items-center justify-content-between">
								<span class="badge bg-secondary-subtle text-secondary rounded-pill">Completed</span>
								<small class="text-dark-emphasis">2 hours ago</small>
							</div>
							<h6 class="fw-semibold text-dark mt-2 mb-1">Mitsubishi Pajero (B 9012 GH)</h6>
							<p class="text-dark-emphasis m-0">Returned to: Politeknik Negeri Jakarta</p>
						</div>
					</div>
				</div>
			</div>
		</div>

		<!-- Right Side: Dashboard Map -->
		<div class="col-8 d-flex flex-column">
			<div class="card border-0 shadow-sm flex-grow-1 d-flex flex-column overflow-hidden">
				<div class="card-body p-0 position-relative flex-grow-1" style="min-height: 350px;">
					<div id="dashboard-map" style="position: absolute; top: 0; bottom: 0; left: 0; right: 0; z-index: 1;"></div>
				</div>
			</div>
		</div>
	</div>
</div>

<script>
(function() {
	function initMap() {
		const mapElement = document.getElementById('dashboard-map');
		if (!mapElement) return;

		const homeLat = -6.370901936057233;
		const homeLng = 106.82459298887727;
		const homeIcon = L.icon({
			iconUrl: '/assets/home.png',
			iconSize: [42, 42],
			iconAnchor: [21, 21],
			popupAnchor: [0, -15]
		});

		const carIcon = L.icon({
			iconUrl: '/assets/plain.png',
			iconSize: [25, 41],
			iconAnchor: [12.5, 41],
			popupAnchor: [0, -35]
		});

		// Initialize Map
		if (window.dashboardMap) {
			try {
				window.dashboardMap.off();
				window.dashboardMap.remove();
			} catch (e) {
				console.error('Error removing map:', e);
			}
			window.dashboardMap = null;
		}

		const map = L.map('dashboard-map').setView([homeLat, homeLng], 13);
		window.dashboardMap = map;

		mapElement.addEventListener('htmx:beforeCleanupElement', function() {
			if (window.dashboardMap) {
				try {
					window.dashboardMap.off();
					window.dashboardMap.remove();
				} catch (e) {
					console.error('Error removing map:', e);
				}
				window.dashboardMap = null;
			}
		});

		L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
			maxZoom: 19,
			attribution: '© OpenStreetMap'
		}).addTo(map);

		// Home Marker
		L.marker([homeLat, homeLng], { icon: homeIcon }).addTo(map).bindPopup('Politeknik Negeri Jakarta');

		// Mock Car Markers
		L.marker([-6.368, 106.828], { icon: carIcon }).addTo(map).bindPopup('Toyota Avanza (B 1234 CD)<br>Status: Heading to Kantor Pusat Jakarta');
		L.marker([-6.375, 106.820], { icon: carIcon }).addTo(map).bindPopup('Honda CR-V (B 5678 EF)<br>Status: Heading to Bandara Soekarno-Hatta');

		map.invalidateSize();
	}

	const isHtmxSwap = document.querySelector('.htmx-swapping, .htmx-settling') !== null;
	if (isHtmxSwap) {
		document.body.addEventListener('htmx:afterSettle', function() {
			initMap();
		}, { once: true });
	} else {
		initMap();
	}
})();
</script>
{% endblock %}
